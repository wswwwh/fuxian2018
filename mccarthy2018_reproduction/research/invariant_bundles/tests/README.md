# Research test map

The executable tests remain in the repository-level `tests/` directory so the
existing discovery command covers them.  The Stage-D suite includes residual,
real-Schur selection, phase alignment, phase permutation, resolution lifting,
complex-pair handling, manifold direction consistency, and stored benchmark
registry reproducibility.

